{
  "version": 2,
  "builds": [
    {
      "src":"*.js",
      "use": "@vercel/node"  // This tells Vercel to use Node.js to run the serverless functions
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest":"/" 
    }
  ]
}
